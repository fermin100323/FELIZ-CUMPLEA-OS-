<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Feliz Cumpleaños, Mi Amor</title>
  <style>
    body {
      margin: 0;
      font-family: 'Segoe Script', cursive;
      background: radial-gradient(circle at top, #ffe4e1, #ffc0cb);
      overflow: hidden;
      height: 100vh;
      position: relative;
      display: flex;
      align-items: center;
      justify-content: center;
      flex-direction: column;
      color: #880e4f;
      text-align: center;
    }

    h1 {
      font-size: 3.5rem;
      margin-bottom: 0.5rem;
      animation: fadeIn 2s ease-in-out;
    }

    h2 {
      font-size: 1.5rem;
      margin-bottom: 1rem;
      animation: fadeIn 2.5s ease-in-out;
    }

    .carta {
      width: 300px;
      height: 200px;
      background: #fff0f5;
      border: 2px solid #ff99cc;
      border-radius: 10px;
      padding: 20px;
      box-shadow: 0 5px 15px rgba(0,0,0,0.2);
      position: relative;
      animation: zoomIn 2s ease forwards;
    }

    .carta::before {
      content: "💌";
      font-size: 2rem;
      position: absolute;
      top: -30px;
      left: 50%;
      transform: translateX(-50%);
    }

    .carta p {
      font-size: 1.2rem;
      line-height: 1.6;
    }

    audio {
      position: absolute;
      bottom: 10px;
      left: 10px;
    }

    .heart {
      position: absolute;
      width: 20px;
      height: 20px;
      background: #ff4b5c;
      transform: rotate(45deg);
      animation: float 8s linear infinite;
    }

    .heart::before,
    .heart::after {
      content: "";
      position: absolute;
      width: 20px;
      height: 20px;
      background: #ff4b5c;
      border-radius: 50%;
    }

    .heart::before {
      top: -10px;
      left: 0;
    }

    .heart::after {
      left: -10px;
      top: 0;
    }

    @keyframes float {
      0% {
        transform: translateY(100vh) rotate(45deg);
        opacity: 1;
      }
      100% {
        transform: translateY(-10vh) rotate(45deg);
        opacity: 0;
      }
    }

    @keyframes fadeIn {
      from {opacity: 0;}
      to {opacity: 1;}
    }

    @keyframes zoomIn {
      0% { transform: scale(0); opacity: 0; }
      100% { transform: scale(1); opacity: 1; }
    }

    footer {
      position: absolute;
      bottom: 20px;
      font-size: 1rem;
      color: #6d214f;
    }
  </style>
</head>
<body>

  <h1>🎉 ¡Feliz Cumpleaños, Mi Amor! 🎂</h1>
  <h2>Para la persona más especial de mi vida 💖</h2>

  <div class="carta">
    <p>
      Hoy celebro tu existencia, tu dulzura, tu risa y todo lo que eres. <br>
      Eres mi mayor regalo, y cada día contigo es magia pura. <br><br>
      ¡Te amo con todo mi corazón, mi princesa! 💕
    </p>
  </div>

  <footer>Con todo mi amor, Tu novio 💘</footer>

  <audio autoplay loop>
    <source src="https://www.bensound.com/bensound-music/bensound-love.mp3" type="audio/mpeg">
    Tu navegador no soporta audio HTML5.
  </audio>

  <script>
    // Corazones flotando
    function crearCorazones() {
      const cantidad = 35;
      for (let i = 0; i < cantidad; i++) {
        const heart = document.createElement("div");
        heart.classList.add("heart");
        heart.style.left = Math.random() * 100 + "vw";
        heart.style.animationDuration = (5 + Math.random() * 5) + "s";
        document.body.appendChild(heart);
        setTimeout(() => heart.remove(), 8000);
      }
    }

    setInterval(crearCorazones, 600);
  </script>

</body>
</html>
